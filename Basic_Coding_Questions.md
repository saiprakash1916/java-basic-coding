These are very common for **Java / logic / coding rounds (0–5 yrs experience)**.

---

## 🔹 String & Number Logic Questions

1. Check whether a given **string is a palindrome** without using reverse methods.
2. Check whether a **number is a palindrome** without converting it to a string.
3. Write a program to check palindrome for **both string and integer using one method**.
4. Check if a string is palindrome **ignoring case and spaces**.
5. Check palindrome using **recursion**.
6. Check palindrome using **two-pointer technique**.
7. Find the **longest palindromic substring** in a given string.
8. Count how many **palindromes** are present in a string.

---

## 🔹 String Manipulation Questions

1. Reverse a string **without using StringBuffer/StringBuilder**.
2. Reverse words in a sentence but **not the characters**.
3. Reverse characters of each word in a sentence.
4. Find **duplicate characters** in a string.
5. Find the **first non-repeated character** in a string.
6. Check if two strings are **anagrams**.
7. Remove duplicate characters from a string.
8. Find the number of **vowels and consonants** in a string.
9. Find all **permutations** of a string.
10. Check if a string contains **only digits**.

---

## 🔹 Number Logic Questions

1. Check whether a number is **Armstrong number**.
2. Check whether a number is **prime**.
3. Print all prime numbers between 1 and N.
4. Find **factorial** of a number (iterative & recursive).
5. Find **Fibonacci series** without recursion.
6. Reverse a number without using string conversion.
7. Find the **sum of digits** of a number.
8. Find the **largest digit** in a number.
9. Check whether a number is **perfect number**.
10. Swap two numbers **without using third variable**.

---

## 🔹 Arrays & Collections

1. Find the **second largest element** in an array.
2. Remove duplicate elements from an array.
3. Find missing number in an array of 1 to N.
4. Sort an array **without using sort()**.
5. Find the frequency of each element in an array.
6. Find common elements between two arrays.
7. Find the **largest and smallest** number in an array.
8. Rotate an array left/right by K positions.

---

## 🔹 Java-Specific Logic Questions

1. Difference between `String`, `StringBuilder`, and `StringBuffer`.
2. Why is `String` immutable?
3. Can we override a **static method**?
4. Can we overload the `main` method?
5. What happens if `equals()` is not overridden?
6. Difference between `==` and `equals()`.
7. Why `HashMap` allows one null key but `Hashtable` does not?
8. How does `HashMap` internally work?
9. What is the difference between **checked and unchecked exceptions**?

---

## 🔹 Coding Round Scenarios

1. Validate input: allow **only numbers or only alphabets**.
2. Count word frequency in a sentence.
3. Find duplicate words in a paragraph.
4. Convert a sentence to **title case**.
5. Remove special characters from a string.

---